plugins {
  id 'lucene-in-action-config'
}

dependencies {
  implementation group: 'org.apache.lucene', name: 'lucene-highlighter', version: '9.5.0'
  testImplementation(project(':analysis'))
  testImplementation(project(':common'))
}