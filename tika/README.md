# tika — Chapter 7: Extracting Text with Tika

This module covers using Apache Tika as a content extraction layer in front of a Lucene index. 

`TikaIndexer` uses `AutoDetectParser` to  extract text and metadata from different file formats e.g. plain text, HTML, RTF, PDF, etc, without any format-specific parsing code.
